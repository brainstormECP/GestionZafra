@model GestionZafra.Models.TiposSectorPropiedad

@{
    ViewBag.Title = "Eliminar";
}
<h3>Esta seguro que desea eliminar este Tipo de Sector de Propiedad?</h3>
<div class="span5">
    <fieldset class="data-fluid">
        <legend>Tipo de Sectores de Propiedad</legend>

        <div class="row-form">
            @Html.DisplayNameFor(model => model.nombreTipoSector):
        <div class="label label-warning">
            @Html.DisplayFor(model => model.nombreTipoSector)
        </div>
        </div>

    </fieldset>
    @using (Html.BeginForm())
    {
        <div class="toolbar bottom tar">
            @Html.ActionLink("Lista de Tipo de Sectores de Propiedad", "Index",null,new{Class="btn"})
            <input class="btn btn-danger" type="submit" value="Eliminar" />
        </div>
    }
</div>

@model GestionZafra.Models.MarcasCombinadas

@{
    ViewBag.Title = "Eliminar";
}
<h3>Esta seguro que desea eliminar esta Marca de Combinada?</h3>
<div class="span5">
    <fieldset class="data-fluid">
        <legend>Marcas de Combinadas</legend>

        <div class="row-form">
            @Html.DisplayNameFor(model => model.nombreCombinada):
        <div class="label label-warning">
            @Html.DisplayFor(model => model.nombreCombinada)
        </div>
        </div>

    </fieldset>
    @using (Html.BeginForm())
    {
        <div class="toolbar bottom tar">
            @Html.ActionLink("Lista de Marcas de Combinadas", "Index",null,new {Class="btn"})
            <input class="btn btn-danger" type="submit" value="Eliminar" />
        </div>
    }
</div>

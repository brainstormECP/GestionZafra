@model GestionZafra.Models.MarcasCombinadas

@{
    ViewBag.Title = "Editar";
}
<div class="span5">
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)

        <fieldset class="data-fluid">
            <legend>Marcas de Combinadas</legend>

            @Html.HiddenFor(model => model.id)

            <div class="row-form">
                @Html.LabelFor(model => model.nombreCombinada)
            </div>
            <div class="row-form">
                @Html.EditorFor(model => model.nombreCombinada)
                @Html.ValidationMessageFor(model => model.nombreCombinada)
            </div>
            <div class="toolbar bottom tar">
                 @Html.ActionLink("Lista de Marcas de Combinadas", "Index", null,new{Class="btn"})
                <input class="btn btn-success" type="submit" value="Guardar" />
            </div>
        </fieldset>
    }
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

@model IEnumerable<GestionZafra.Models.ParqueEquipos>

@{
    ViewBag.Title = "Parque de Equipos";
    ViewBag.Message = "Parque de Equipos";
}

<div class="content">
    <div class="row-fluid">
        <div class="span12">
            <div class="block">
                <div class="head dblue">
                    <div class="icon"><span class="ico-layout-9"></span></div>
                    @Html.ActionLink("Nuevo Parque", "Create")
                </div>
                <div class="data-fluid">
                    <table class="table fpTable lcnp" cellpadding="0" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Suministradores.nombreSuministrador)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.cantidadEquipos)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.TipoEquipos.descripcionEquipo)
                                </th>
                                <th width="80" class="TAC">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Suministradores.TiposSectorPropiedad.nombreTipoSector)
                                        @Html.DisplayFor(modelItem => item.Suministradores.nombreSuministrador)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.cantidadEquipos)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TipoEquipos.descripcionEquipo)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Edit","ParqueEquipos")/@item.id" class="button green">
                                            <div class="icon"><span class="ico-pencil"></span></div>
                                        </a>
                                        <a href="@Url.Action("Delete","ParqueEquipos")/@item.id" class="button red">
                                            <div class="icon"><span class="ico-remove"></span></div>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
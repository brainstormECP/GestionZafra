@model IEnumerable<GestionZafra.Models.Suministradores>

@{
    ViewBag.Title = "Suministradores";
    ViewBag.Message = "Suministradores";
}
<div class="content">
    <div class="row-fluid">
        <div class="span12">
            <div class="block">
                <div class="head dblue">
                    <div class="icon"><span class="ico-layout-9"></span></div>
                    @Html.ActionLink("Nuevo Suministrador", "Create")
                </div>
                <div class="data-fluid">
                    <table class="table fpTable lcnp" cellpadding="0" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.TiposSectorPropiedad.nombreTipoSector)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.nombreSuministrador)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.activo)
                                </th>
                                <th width="80" class="TAC">Accciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TiposSectorPropiedad.nombreTipoSector)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.nombreSuministrador)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.activo)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Edit","Suministradores")/@item.id" class="button green">
                                            <div class="icon"><span class="ico-pencil"></span></div>
                                        </a>
                                        <a href="@Url.Action("Delete","Suministradores")/@item.id" class="button red">
                                            <div class="icon"><span class="ico-remove"></span></div>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@model GestionZafra.Models.EstadoEquipo

@{
    ViewBag.Title = "Eliminar";
}
<h3>Esta seguro que desea eliminar este Estado de Equipo?</h3>
<div class="span5">
    <fieldset class="data-fluid">
        <legend>Estados de Equipo</legend>

        <div class="row-form">
            @Html.DisplayNameFor(model => model.nombreEstado):
        <div class="label label-warning">
            @Html.DisplayFor(model => model.nombreEstado)
        </div>
        </div>

    </fieldset>
    @using (Html.BeginForm())
    {<div class="toolbar bottom tar">
        @Html.ActionLink("Lista de Estados de Equipo", "Index", null, new { Class = "btn" })
        <input class="btn btn-danger" type="submit" value="Eliminar" />
    </div>
    }
</div>

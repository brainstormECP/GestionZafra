@model IEnumerable<GestionZafra.Models.Campo>

@{
    ViewBag.Title = "Campos";
    ViewBag.Message = "Campos";
}
<div class="content">
    <div class="row-fluid">
        <div class="span12">
            <div class="block">
                <div class="head dblue">
                    @Html.ActionLink("Nuevo Campo", "Create")
                </div>
                <div class="data-fluid">
                    <table class="table fpTable lcnp" cellpadding="0" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Suministradores.nombreSuministrador)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.cantCanaVerde)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.cantCanaQuemada)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.cepa)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.VariedadCana.nombreVariedad)
                                </th>
                                <th width="80" class="TAC">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Suministradores.nombreSuministrador)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.cantCanaVerde)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.cantCanaQuemada)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.cepa)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.VariedadCana.nombreVariedad)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Edit","Campo")/@item.id" class="button green">
                                            <div class="icon"><span class="ico-pencil"></span></div>
                                        </a>
                                        <a href="@Url.Action("Delete","Campo")/@item.id" class="button red">
                                            <div class="icon"><span class="ico-remove"></span></div>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>